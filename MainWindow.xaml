<Window x:Class="SoundBoardLite.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SoundBoardLite"
        xmlns:viewmodels="clr-namespace:SoundBoardLite.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="SoundBoard Lite" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Closing="Window_Closing">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <tb:TaskbarIcon x:Name="TrayIcon"
                        IconSource="/Assets/icon.ico"
                        ToolTipText="SoundBoard Lite"
                        TrayMouseDoubleClick="TrayIcon_TrayMouseDoubleClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Pokaż okno" Click="MenuItem_Show_Click" />
                    <MenuItem Header="Zamknij" Click="MenuItem_Quit_Click" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
        
        <!-- Header / Toolbar -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" Grid.Row="0" materialDesign:ElevationAssist.Elevation="Dp4">
            <DockPanel>
                <TextBlock VerticalAlignment="Center" Margin="16 0 0 0" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Text="SoundBoard Lite"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <TextBlock Text="Wyjście Audio:" VerticalAlignment="Center" Margin="0 0 8 0" Foreground="White" />
                    <ComboBox ItemsSource="{Binding AudioDevices}" 
                              SelectedItem="{Binding SelectedAudioDevice}" 
                              DisplayMemberPath="Name" 
                              Width="250" 
                              Foreground="White"
                              materialDesign:HintAssist.Hint="Wybierz urządzenie"
                              Margin="0 0 16 0"/>
                    <Button Content="Dodaj Dźwięk" Command="{Binding AddSoundCommand}" />
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- Dashboard / Grid of Sounds -->
        <ScrollViewer Grid.Row="1" Margin="16" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Sounds}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Width="220" Margin="8" Padding="12" materialDesign:ElevationAssist.Elevation="Dp2">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" TextTrimming="CharacterEllipsis" Margin="0 0 0 8"/>
                                <TextBox Text="{Binding Shortcut}" materialDesign:HintAssist.Hint="Skrót (np. F1, Ctrl+D)" Margin="0 0 0 8"/>
                                
                                <Grid Margin="0 8 0 8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <materialDesign:PackIcon Kind="VolumeHigh" VerticalAlignment="Center" />
                                    <Slider Grid.Column="1" Minimum="0" Maximum="1" Value="{Binding Volume}" Margin="8 0 0 0" />
                                </Grid>
                                
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="Zapisz" 
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            Command="{Binding DataContext.SaveSoundCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                            CommandParameter="{Binding}" 
                                            Margin="0 8 8 0" />
                                    <Button Content="Odtwórz" 
                                            Command="{Binding DataContext.PlaySoundCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                            CommandParameter="{Binding}" 
                                            Margin="0 8 0 0" />
                                </StackPanel>
                            </StackPanel>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
